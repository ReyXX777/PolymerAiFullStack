import random

def mock_polymer_prediction(structure, additives, temperature):
    # Mock function to simulate predictions
    return {
        "glass_transition_temp": random.uniform(100, 500),
        "mechanical_strength": random.uniform(50, 300),
        "thermal_stability": random.uniform(200, 600)
    }
